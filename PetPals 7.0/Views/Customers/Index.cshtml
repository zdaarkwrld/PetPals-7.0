@model IEnumerable<PetPals_7._0.Models.Customers>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomersName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomersSurname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomersName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomersSurname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomersId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomersId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomersId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
